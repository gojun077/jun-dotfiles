#!/bin/bash
#~/.bash_profile
#
# Config for login shells (run at initial connection)

if [ -f ~/.bashrc ]; then
  #shellcheck source=/home/junbuntu/.bashrc
  source ~/.bashrc
fi

# For i3 WM ssh-agent
ssh-add ~/.ssh/archjun_rsa
