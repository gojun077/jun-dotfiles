# bash_profile_mac
# This file should be symlinked to ~/.bash_profile
#
# Created on: Sometime in 2021
# Last Updated: Tue 21 Jan 2026
# Updated by: gopeterjun@naver.com
#
# Note: MacOS behavior around `.bash_profile` is different from
# that of Linux and other POSIX systems. Normally, `.bash_profile`
# is only executed by the login shell, but `Terminal.app` on MacOS
# runs a login shell by default for *each* new terminal window.
#
# Terminal.app, Ghostty.app, etc your default shell as a *login shell*,
# which reads '.bash_profile' and launches '-bash' (dash prefix) in the
# process list. Non-login shells on MacOS read '.bashrc' and don't use a
# dash prefix.


export GOROOT=/usr/local/go
export GOPATH="/Users/$USER/go"
export HOMEBREW_PREFIX=/opt/homebrew
export HOMEBREW_CELLAR=/opt/homebrew/Cellar
export PYENV_ROOT="$HOME/.pyenv"
export XDG_CONFIG_HOME="$HOME/.config"
export PNPM_HOME="$HOME/Library/pnpm"

export PATH="$HOME/.local/bin:$HOME/bin:/usr/local/bin:/System/Cryptexes/App/usr/bin:/usr/bin:/bin:/usr/sbin:/sbin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/local/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/bin:/var/run/com.apple.security.cryptexd/codex.system/bootstrap/usr/appleinternal/bin:/opt/pmk/env/global/bin:/user/local/MacGPG2/bin:/Library/TeX/texbin:/opt/homebrew/bin:/Applications/Ghostty.app/Contents/MacOS:$PYENV_ROOT/bin:$GOROOT/bin:$GOPATH/bin:$HOME/.krew/bin:$HOME/bin-pkg:$PNPM_HOME"

eval "$(pyenv init --path)"

if [ -f ~/.bashrc ]; then
  # SC override is to quiet the warning about following path to source
  # shellcheck disable=SC1091
  source "$HOME"/.bashrc
fi
